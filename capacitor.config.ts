import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alsid66.softethervpn',
  appName: 'SoftEther VPN Manager',
  webDir: 'dist'
};

export default config;
